# deckpilot

Consulting-grade PowerPoint status decks generated from structured programme data.

Work in progress. See `pyproject.toml` for the dependency set and `deckpilot/theme/tokens.py`
for the design system.
